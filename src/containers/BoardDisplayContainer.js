import React, { Component } from "react";

class BoardDisplayContainer extends Component {
  render() {
    console.log(this.props.boardId);
    return <div />;
  }
}

export default BoardDisplayContainer;
